name: Auto Trigger Build Openwrt

on:
  schedule:
    - cron: '0 2 * * *'  # Setiap hari jam 02:00 UTC
  workflow_dispatch:

jobs:
  update-readme:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Update README.md
        run: |
          sed -i -E "s|Build_Latest-[0-9]{8}-blue|Build_Latest-$(date +'%Y%m%d')-blue|g" README.md
          sed -i -E "s|Amlogic_Latest-[0-9]{8}-blue|Amlogic_Latest-$(date +'%Y%m%d')-blue|g" README.md
          sed -i -E "s|Build_Snapshots-[0-9]{8}-blue|Build_Snapshots-$(date +'%Y%m%d')-blue|g" README.md
          sed -i -E "s|Amlogic_Snapshots-[0-9]{8}-blue|Amlogic_Snapshots-$(date +'%Y%m%d')-blue|g" README.md
      
      - name: Commit changes
        run: |
          git config --global user.name 'github-actions'
          git config --global user.email 'github-actions@github.com'
          git add README.md
          git commit -m "Auto Update $(date +'%Y-%m-%d %H:%M:%S')"
          git push

  trigger-generate-rtawrt-latest:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger generate-rtawrt
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: generate-rtawrt.yml
          repo: rtaserver/MyOpenWRT
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main
          inputs: '{"release_branch": "23.05.4", "tunnel": "openclash-passwall"}'
  
  trigger-generate-rtawrt-amlogic-latest:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger generate-rtawrt-amlogic
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: generate-rtawrt-amlogic.yml
          repo: rtaserver/MyOpenWRT
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main
          inputs: '{"release_branch": "23.05.4", "tunnel": "openclash-passwall"}'

  trigger-generate-rtawrt-snapshots:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger generate-rtawrt
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: generate-rtawrt.yml
          repo: rtaserver/MyOpenWRT
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main
          inputs: '{"release_branch": "snapshots", "tunnel": "openclash"}'
  
  trigger-generate-rtawrt-amlogic-snapshots:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger generate-rtawrt-amlogic
        uses: benc-uk/workflow-dispatch@v1
        with:
          workflow: generate-rtawrt-amlogic.yml
          repo: rtaserver/MyOpenWRT
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: main
          inputs: '{"release_branch": "snapshots", "tunnel": "openclash"}'

  del_runs:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read
    steps:
      - name: Delete old workflow runs
        uses: Mattraks/delete-workflow-runs@v2
        with:
          token: ${{ github.token }}
          repository: ${{ github.repository }}
          retain_days: 0
          keep_minimum_runs: 1